# Frontend

原生 HTML/CSS/JavaScript 实现的学生管理页面，包含：

- 学生列表 + 新增/编辑弹窗
- 签到弹窗（含剩余课时为 0 的禁用控制）
- 签到记录查询（按学生、日期范围过滤）

## 启动

请先启动后端服务：

```bash
cd backend
npm install
npm start
```

然后访问 `http://localhost:3000`。
